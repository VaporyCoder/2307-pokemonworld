import React, { useState } from "react";

const Create = ({createPoke, createTrainer}) => {
    const [newPoke, setNewPoke] = useState("");
    const [newTrain, setNewTrain] = useState("");
}

export default Create;